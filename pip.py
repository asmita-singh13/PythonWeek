#pandas is not an built in module. It is external package
#so we have used it here and installed it in the terminal
# we used: pip install pandas
#then we were succesully able to run the code
#hashlib is built in, so we did not need install it 

import pandas as pd
import hashlib
print("hi")
